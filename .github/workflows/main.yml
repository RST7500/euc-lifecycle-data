name: Test Workflow

on:
  push:
    branches:
      - main

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - name: Display message
        run: echo "Hello from GitHub Actions"
